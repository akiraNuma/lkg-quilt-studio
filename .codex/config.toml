# CLAUDE.md is authoritative; when AGENTS.md takes precedence, follow its references.
project_doc_fallback_filenames = ["CLAUDE.md"]

approval_policy = "never"
sandbox_mode = "danger-full-access"

[features]
hooks = true
